[package]
name = "bee-trace-common"
version = "0.1.0"
edition = "2021"

license.workspace = true

[features]
default = []
user = ["aya"]

[dependencies]
aya = { workspace = true, optional = true }

[dev-dependencies]
assert_matches = { workspace = true }

[lib]
path = "src/lib.rs"
